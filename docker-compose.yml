services:
  vibey:
    build: .
    ports:
      - "5353:5353"
      - "1455:1455"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - vibey-data:/app/data
    labels:
      - vibey=master
    environment:
      - VIBEY_DOCKER=1
    network_mode: bridge

volumes:
  vibey-data:
